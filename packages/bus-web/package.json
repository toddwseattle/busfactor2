{
  "name": "bus-web",
  "version": "0.0.0",
  "private": true,
  "description": "React and Vite web app for Busfactor2.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "dev": "vite",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "bus-lib": "0.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
